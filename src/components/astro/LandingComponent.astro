---
import Layout from '../../layouts/Layout.astro';
import '../../styles/variables.css';
import laptopMockup from '../../assets/MaskGroup2.png';
---

<Layout>
	<section class="hero">
		<div class="hero__top">
			<div class="hero__content">
				<p class="hero__eyebrow">STRATEŠKE STORITVE</p>
				<h1 class="hero__headline">
					Zgradimo ti spletno prisotnost, ki prodaja.
				</h1>
				<p class="hero__supporting">
					Ni dovolj, da te najdejo – pomembno je, da ostanejo in kupijo.
				</p>
			</div>
		</div>

		<div class="hero__bottom">
			<div class="hero__content hero__content--bottom">
				<p class="hero__description">
					Podjetja pogosto zapravljajo tisoče evrov za spletne strani, ki ne
					pretvarjajo obiskovalcev v kupce. Naš pristop poveže dizajn,
					uporabniško izkušnjo in strategijo v funkcionalno prodajno orodje.
				</p>
			</div>
		</div>

		<img src={laptopMockup.src} alt="MacBook Air mockup" class="hero__laptop" loading="lazy" />
	</section>
</Layout>

<style>
	:global(body) {
		background-color: #000;
		color: #fff;
		margin: 0;
	}

	.hero {
		position: relative;
		overflow: hidden;
		min-height: 100vh;
		display: grid;
		grid-template-rows: auto auto;
		background: #0f0f0f url({backgroundPattern}) center/auto repeat;
	}

	.hero__top {
		background: #0f0f0f url({backgroundPattern}) center/auto repeat;
		padding-bottom: 0%;
	}

	.hero__bottom {
		background: var(--unnamed-color-885dfd, #885dfd);
		clip-path: polygon(0 70%, 100% 0, 100% 100%, 0 100%);
		padding: clamp(6rem, 10vw, 12rem) clamp(1.5rem, 8vw, 8rem) clamp(4rem, 8vw, 8rem);
		display: flex;
		align-items: flex-end;
	}

	.hero__content {
		max-width: 48rem;
		margin-top: 200px;
		margin-left: clamp(64px, 10vw, 140px);
	}

	.hero__content--bottom {
		margin-left: auto;
		text-align: right;
	}

	.hero__eyebrow {
		width: 536px;
		height: 12px;
		margin-bottom: 50px;
		text-align: left;
		font: normal normal normal 14px/16px PP Telegraf Ultralight;
		letter-spacing: 2.1px;
		color: #FFFFFF;
		text-transform: uppercase;
		opacity: 1;
	}

	.hero__headline {
		width: 1088px;
		height: 204px;
		text-align: left;
		font: normal normal 800 100px/117px PP Telegraf;
		letter-spacing: 0px;
		color: #FFFFFF;
		opacity: 1;
	}

	.hero__supporting {
		margin: 1.5rem 0 0;
		max-width: 28rem;
		font-size: clamp(1.125rem, 2.5vw, 1.5rem);
		line-height: 1.6;
		color: rgba(255, 255, 255, 0.75);
	}

	.hero__description {
		width: 674px;
        height: 295px;
		text-align: left;
		font: normal normal 800 34px/40px PP Telegraf;
		letter-spacing: 0px;
		color: #FCFFF5;
		opacity: 1;
	}

	.hero__laptop {
		position: absolute;
		bottom: clamp(2rem, 6vw, 4rem);
		width: 776px;
		height: 656px;
		padding-top: 100px;
		margin-left: 100px;
		max-width: 90%;
		z-index: 2;
		filter: drop-shadow(0 1.75rem 3rem rgba(0, 0, 0, 0.45));
	}

	@media (max-width: 900px) {
		.hero {
			min-height: auto;
			grid-template-rows: auto auto;
		}

		.hero__bottom {
			clip-path: polygon(0 10%, 100% 0, 100% 100%, 0 100%);
		}

		.hero__content--bottom {
			text-align: left;
			margin-left: 0;
			max-width: 100%;
		}

		.hero__laptop {
			position: static;
			transform: none;
			margin: clamp(2rem, 5vw, 3rem) auto 0;
			display: block;
		}
	}
</style>